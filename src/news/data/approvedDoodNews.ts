import type { DoodNewsDraft } from "../types";

// Static mode source of truth.
// Add manually reviewed and approved items here before building the public site.
export const approvedDoodNews: DoodNewsDraft[] = [];
